# Experiment records

Record dataset revision, tokenizer/version, mixture, model config, seed,
effective batch size, sequence length, LR schedule, total tokens, train/validation
loss, Korean/English validation perplexity, tokens/sec, peak VRAM and wall time.
